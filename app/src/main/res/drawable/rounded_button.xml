<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/purple" /> <!-- Button background color -->
    <corners android:radius="@dimen/btn_radius"/> <!-- Radius for rounded corners -->
    <size android:height="@dimen/login_btn_height"/>
</shape>
